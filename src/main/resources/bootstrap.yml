spring:
  profiles:
    active: dev
  output:
    ansi:
      enabled: always
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.draper.itoken.web.account.domain
  global-config:
    banner: false